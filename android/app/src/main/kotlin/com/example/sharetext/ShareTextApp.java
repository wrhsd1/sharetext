package com.example.sharetext;

import io.flutter.app.FlutterApplication;

// 继承FlutterApplication以支持Android嵌入V2
public class ShareTextApp extends FlutterApplication {
    @Override
    public void onCreate() {
        super.onCreate();
    }
}
